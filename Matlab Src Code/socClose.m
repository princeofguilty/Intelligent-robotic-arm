% CLIENT connect to a server and read a message
%
% Usage - message = client(host, port, number_of_retries)
function socClose(input_socket)

    import java.net.Socket
    import java.io.*
    input_socket.close;
end